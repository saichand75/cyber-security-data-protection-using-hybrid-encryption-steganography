create database cybersecurity;
use cybersecurity;

create table register(username varchar(50) primary key,
password varchar(50),
contact_no varchar(20),
email varchar(70), 
address varchar(80));

create table files(username varchar(50), filename varchar(50), hashcode varchar(150), upload_date varchar(30), upload_type varchar(50));

